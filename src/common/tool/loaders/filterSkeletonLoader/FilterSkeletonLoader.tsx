import React from "react";
import styles from "./FilterSkeletonLoader.module.css";
type Props = {};

const FilterSkeletonLoader = (props: Props) => {
  return <div className={styles["container"]}></div>;
};

export default FilterSkeletonLoader;
