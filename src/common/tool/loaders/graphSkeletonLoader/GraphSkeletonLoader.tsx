import React from "react";
import styles from "./GraphSkeletonLoader.module.css";
type Props = {};

const GraphSkeletonLoader = (props: Props) => {
  return <div className={styles["container"]}></div>;
};

export default GraphSkeletonLoader;
